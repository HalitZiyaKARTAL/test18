use this folder,
you can make subfolders too,
after pushing updates you can use link hosted from here,
if deployment lags etc feel free to use githack for less waiting,
for data retention use apps export import to continue easiest,
use sub folders for experiments too,
i can use experimental versions to test etc too and if i like it use as main too,
try not to delete existing stuff, use can use date as subfolder names too for example etc etc
