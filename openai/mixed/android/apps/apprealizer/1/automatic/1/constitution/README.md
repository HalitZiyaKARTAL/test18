# Constitution

The files in `current/` are preserved verbatim and must be read completely before implementation work.

| File | Role |
| --- | --- |
| `chameleon_app_realizer_consolidation_prompt_20260731_v1.txt` | Detailed consolidation constitution: native-shell boundary, exact editor, bridge/sandbox/auth/policy semantics, Android portals, lifecycle, compression rules, and required validation. |
| `html_realizer_zero_shot_prompt_20260731_v1.txt` | Compact product prompt. It explicitly defines the four Graylist choices: Always allow, Allow once, Reject once, and Always reject. |

Current explicit user instructions always win. Where the two saved prompts differ in specificity, retain both and apply the later or more explicit requirement unless the user states otherwise. In particular, the four-choice Graylist behavior in the compact prompt is the required current UI contract.

Historical design documents in Arena remain historical references and do not outrank this directory.
